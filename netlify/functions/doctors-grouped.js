// Standalone function for backward compatibility
// Note: This already exists as getDoctors.js but with different path
export { handler } from './getDoctors.js';

export const config = {
    path: "/doctors/grouped"
};
